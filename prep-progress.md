# System Design Prep — Progress Log

## Topics Covered
<!-- date | topic | source -->
2026-07-23 | Video Streaming (Netflix/YouTube) — upload pipeline, transcoding, ABR/HLS, CDN, storage, dedup | own notes (`interviews/video-streaming/`) + recent git commits
<!-- 2026-07-23 was an automated check-in, no live user. Topic inferred from git history (latest commits added video-streaming diagrams + answers). Confirm/correct on next live run. -->
2026-07-24 | Ride-Sharing (Uber/Lyft) — geospatial indexing (geohash/S2/H3), location ingest at scale, matching scoring, trip state machine, real-time tracking, surge pricing | own notes (`interviews/ride-sharing/`) + git (added diagrams.md, simple-diagram.md, README)
<!-- 2026-07-24 automated check-in, no live user. Topic inferred from today's commits (ride-sharing diagrams + README). Self-check quiz issued in chat. Confirm/grade on next live run. -->
2026-07-25 | CDN & Edge (review) — hit-ratio/origin capacity math, Anycast vs GeoDNS, max-age vs s-maxage, surrogate keys, thundering-herd mitigations | own notes (`interviews/cdn-edge/` — README, questions.md, deep-dive.md already complete) — top logged gap
<!-- 2026-07-25 automated check-in, no live user. No new git/file activity today; picked top open gap (CDN & Edge review). 5-Q self-check quiz issued in chat with model answers. Confirm/grade on next live run. -->
2026-07-26 | Blob / Object Storage (Design S3) — durability via erasure coding, presigned URLs + multipart upload, metadata/data-plane separation, read-after-write consistency, event-driven post-processing | own notes (`blob-storage/s3-grill-me-answers.md`, `key-technologies-notes.md` §2 Blob Storage)
<!-- 2026-07-26 automated check-in, no live user. No new git/file activity today; picked top open gap (Blob Storage — P2, dependency of video streaming). 5-Q self-check quiz issued in chat (4 conceptual + 1 mini-design) with model answers. Confirm/grade on next live run. -->
2026-07-27 | Social Feed (Twitter/X home timeline) — fan-out on write vs read, hybrid fan-out threshold, celebrity problem, Redis sorted-set timelines (store IDs), real-time push + thundering herd, chronological → ranked feeds | own notes (`interviews/social-feed/` — README, questions.md 42 Qs, answers.md, deep-dive.md all complete)
<!-- 2026-07-27 automated check-in, no live user. Topic inferred from today's file activity: `interviews/social-feed/Architecture_of_the_global_social_feed.m4a` touched today (audio study material). 5-Q self-check quiz issued in chat (4 conceptual + 1 mini-design) with model answers. Confirm/grade on next live run. -->
2026-07-27 | Collaborative Editing (Google Sheets / Docs) — AUTHORED full new-spec folder live. Central split: sync plane (concurrent edits → converge, OT vs CRDT) vs calc plane (formula dependency DAG → deterministic recompute). Signature hard problem: structural ops (row/col insert) shifting every address + reference → stable identities. Single-writer doc-servers + lease/fencing; snapshot + op-log; presence on separate channel; selective per-user undo; optimistic OT client + virtualized canvas grid. | own notes (`interviews/collaborative-editing/` — README, simple-diagram, questions.md 42+6 Qs, answers.md, diagrams.md 11 diagrams, deep-dive.md all complete)
<!-- 2026-07-27 LIVE session with user. Built collaborative-editing per docs/instructions.md 7-file spec (skipped conducive-sentences by request). Fills ROADMAP Problem 18. Cross-linked to consensus, communication-protocols/chat-system/sse, storage-engines, file-storage, distributed-caching, ride-sharing, distributed-transactions. No quiz issued — authoring session, not study session. -->
2026-07-27 | E-Commerce Platform (Amazon / Shopify) — AUTHORED full new-spec folder live (umbrella topic). Central split: the CONSISTENCY GRADIENT — browse (eventual, cache/CDN) → cart (available, AP/Dynamo) → checkout (strong: no-oversell guarded decrement + no-double-charge idempotency key) → fulfillment (async saga + transactional outbox). Cart = canonical AP example (kv-store); no-oversell/flash-sale (seat-reservation); saga/idempotency (distributed-transactions); outbox (message-queues); reads dominate ~1000:1. Payment depth delegated to planned payment-system (Problem 16). | own notes (`interviews/e-commerce/` — README, simple-diagram, questions.md 42+6 Qs, answers.md, diagrams.md 11 diagrams, deep-dive.md all complete)
<!-- 2026-07-27 LIVE session with user. Built e-commerce as new ROADMAP Problem 19 (added to roadmap; P1). Umbrella topic reusing kv-store/seat-reservation/distributed-transactions/message-queues/distributed-caching/cdn-edge/search-autocomplete/recommendation-system/sharding-replication/rate-limiting/observability. deep-dive.md + README.md written in parallel via subagents. No quiz issued — authoring session. -->
2026-07-29 | Distributed Key-Value Store (Dynamo-style) — quorum math (W+R>N, sloppy quorum), replication + preference list, conflict resolution (LWW clock-skew failure vs vector clocks + siblings vs CRDTs), anti-entropy (read repair + Merkle trees), hot-key/hot-partition. Reinforces the AP cart from e-commerce (7/27) and consistent-hashing/storage-engines patterns. | own notes (`interviews/kv-store/` — README, questions.md, answers.md, deep-dive.md all complete)
<!-- 2026-07-29 automated check-in, no live user. No new git/file activity today; picked kv-store — fully-grounded complete folder, never quizzed, canonical staff-level topic, ties into his AWS/DynamoDB background + the e-commerce cart built 7/27. 5-Q self-check quiz issued in chat (4 conceptual + 1 mini-design) with model answers, grounded in questions.md Q13-Q22. Confirm/grade on next live run. -->
2026-07-28 | Seat Reservation / Ticketmaster (UPGRADE to new-spec) — added simple-diagram.md + diagrams.md (10 diagrams) and extended with Level 9 (Digital Tickets, QR & Notifications) + Level 10 (Frontend Architecture), Q43–Q53. Central split = two-tier: ephemeral hold (Redis SETNX+TTL) vs durable booking (SQL/ACID), fronted by virtual waiting room; read vs write path. QR = signed/rotating token + OFFLINE gate validation (scanner holds signing key + revocation list, scan-once); wallet passes; idempotent reminders. Frontend: 80k-seat interactive map (Canvas/WebGL + LOD/virtualization), real-time lock status via per-section WebSocket deltas, checkout countdown vs authoritative server TTL, WCAG accessibility. | own notes (`interviews/seat-reservation/`)
<!-- 2026-07-28 LIVE session with user. Answered "do we have Ticketmaster/BookMyShow?" → yes (seat-reservation), but it lacked frontend + QR/ticket + diagrams. User chose full new-spec upgrade. Built via Workflow (ultracode on): two draft→adversarial-verify pipelines (answers) + two write→lint pipelines (diagram files). ROADMAP Problem 5 → Done; dashboard 20 done. No quiz — authoring session. -->
2026-07-29 | Payment System (Stripe / PayPal / Google Pay) — AUTHORED full new-spec folder. Central split: INTERNAL ledger truth (exactly-once, append-only, auditable, strongly consistent) vs EXTERNAL PSP reality (unreliable, async, only eventually knowable) → be exact inside, defensive at the boundary, reconcile continuously. Corollary: NEVER TRUST A TIMEOUT (a timeout is an UNKNOWN — retry with the SAME idem key, query by key, else park in INDETERMINATE). Covers: idempotency via UNIQUE-in-txn + stored response + IN_FLIGHT (loser never calls PSP); double-entry ledger with balanced DR/CR, append-only reversals, integer minor units, snapshot+delta balances + drift checker, 7 invariants; webhooks (HMAC over raw body before parsing, freshness, dedupe, 200-fast) as latency optimization NOT truth; 3-way reconciliation + mismatch taxonomy incl. cut-off false positive; refunds (own idem keys! + Σrefunds≤captured) vs chargebacks; payouts from AVAILABLE not pending; PCI scope via tokenization; fraud false-decline asymmetry; FX rate captured at quote. Frontend L9: cross-origin iframe as the real boundary, session-replay leak, one idem key per checkout, 3-DS resume server-side, never optimistically render SETTLEMENT. | own notes (`interviews/payment-system/` — 6 files, ~208KB)
<!-- 2026-07-29 LIVE session. Topic PICKED BY EVIDENCE: ran a 4-agent web-research workflow ranking all 12 remaining topics by real interview frequency; Payment System came out #1 with HIGH confidence (only topic with first-person Blind reports naming Stripe/PayPal; Alex Xu Vol 2 Ch.11; hellointerview breakdown; Grokking; all 4 agents surfaced it). Ranking saved to memory as next-topics-frequency-ranking.md — NEXT UP is Ad Click Aggregation (also high, 4/4 agents, Alex Xu Vol 2 Ch.6). ROADMAP Problem 16 → Done and upgraded P2→P1 on that evidence; dashboard 21 done / 12 not started. Also resolved 11 stale "payment-system not yet built" claims in e-commerce → real links. NOTE: 5 parallel subagents all died with "Connection closed mid-response" trying to emit one large file each — authored all 6 files directly in incremental chunks instead. No quiz — authoring session. -->
2026-08-02 | Food Delivery (DoorDash / Swiggy / Uber Eats) — three distinct traffic paths (browse=read-heavy eventual/CDN, order=strong/atomic, courier-location=write-heavy 125K/s); 3-sided marketplace (restaurant as first-class actor w/ accept/reject/timeout); menu model + real-time "sold out" propagation + browse-vs-checkout consistency split; atomic order + idempotency-key + authorize-then-capture; event-driven saga + transactional outbox for the multi-party order lifecycle; PREP-AWARE just-in-time courier dispatch (assign when food nearly ready, not at placement) + batching/stacked deliveries; composite ETA (prep+pickup+delivery legs); live tracking transport (WS/SSE vs courier GPS upload); peak 5× dinner-rush degradation. | own notes (`interviews/food-delivery/` — README, questions.md 44+6 Qs, answers.md, conducive-sentences.md, deep-dive.md, diagrams.md all complete)
<!-- 2026-08-02 automated check-in, no live user. Topic PICKED BY EVIDENCE: today's git commit "feat: add images for food delivery interview concepts and architecture" added 5 architecture images (three_traffic_paths, menu_architecture_availability_overlay, jit_courier_dispatch_timeline, event_driven_saga_outbox_pattern, authorize_then_capture_payment_flow); diagrams.md modified today; The_Software_Architecture_of_Food_Delivery.m4a audio touched 7/31. Fully-grounded complete folder, never quizzed, high-frequency staff topic ties into his ride-sharing (7/24) geospatial + e-commerce (7/27) consistency-gradient + payment-system (7/29) authorize-then-capture + message-queues saga/outbox work. 5-Q self-check quiz issued in chat (4 conceptual + 1 mini-design, RADIO-framed), weighted to the 5 images he made today, grounded in questions.md Q2/Q8/Q17/Q22/Q25/Q18. Confirm/grade on next live run. -->

2026-08-03 | RADIO Framework + E-Commerce & Food-Delivery RADIO walkthroughs (LIVE authoring) — created a reusable interview-performance guide and one worked backend-HLD walkthrough per topic, every number derived (assumption → arithmetic → the decision it drives). | own notes (`docs/RADIO_FRAMEWORK.md`, `interviews/e-commerce/radio-walkthrough.md`, `interviews/food-delivery/radio-walkthrough.md`, `key-technologies-notes.md` Part III)
<!-- 2026-08-03 LIVE session with user. Request: cross-check e-commerce + food-delivery folders, add use-case ("when to use what") revision layer, write a RADIO instructions file (none existed), and produce RADIO-structured interview walkthroughs with defended numbers. User chose BOTH topics + BACKEND-HLD flavor (confirmed mid-turn: "backend interview mostly"). Deliverables: (1) docs/RADIO_FRAMEWORK.md — R/A/D/I/O phases with time budget, backend-first with brief FE-mapping asides, an estimation toolkit (DAU→QPS→storage→Little's-Law concurrency, constants, how to defend a number), a one-screen checklist, and a failure-patterns table; (2) key-technologies-notes.md Part III "When to Use What (Situational)" §21–§25 — DB-type / caching / replication / consistency-model / distributed-pattern decision tables, each anchored to concrete e-commerce & food-delivery scenarios; (3) two radio-walkthrough.md files. FOLLOW-UP same session: converted all ASCII diagrams in both walkthroughs to color-coded Mermaid (architecture flowchart + end-to-end sequenceDiagram + saga flowchart per topic, matching diagrams.md conventions), and appended a "THE ONE TO DRAW IN THE INTERVIEW" final consolidated whiteboard diagram to BOTH diagrams.md — every datastore labeled by TYPE (RDBMS/cache+CDN/AP-KV/inverted-index/in-mem-TTL-geo/log-stream) with a store-type legend table + a numbered draw-order + narration script. SECOND FOLLOW-UP same session: user supplied a "Use Cases for Redundancy and Replication" doc (17 systems × replication pattern). Integrated it FULL-DISTRIBUTION: (a) de-orphaned + OCR-cleaned fundamentals/Use_Cases_for_Redundancy_and_Replication.md (fixed $ightarrow$/CR + $RPO$/$W+R>N$ latex artifacts), added a hub/accuracy note + a "where these live in this repo" folder map; (b) added a "Replication use-case matrix" (17 rows, each cross-linked to its folder) + RPO/RTO definitions to key-technologies-notes.md §12; (c) appended a "🔁 Redundancy & Replication — how THIS system does it" callout (pattern/mode/why) to 14 deep-dive.md files (url-shortener, file-storage, chat-system, kv-store, social-feed, message-queues, video-streaming, cdn-edge, search-autocomplete, rate-limiting, web-crawler, ride-sharing, seat-reservation + sharding-replication as concept home). FLAGGED as illustrative (not verified): Messenger=HBase→MyRocks not Cassandra, Netflix=Cassandra/EVCache not CockroachDB, Uber Ringpop=SWIM-gossip membership not "ring buffers." CROSS-CHECK result: both folders complete & consistent — payment-system no longer called "planned" (already resolved), all cross-links resolve, 20M-orders/day baseline consistent (e-commerce ~40× Prime-Day peak vs food-delivery ~5× dinner-rush, both defensible). Key derived numbers to be able to reproduce cold: food-delivery ~231 orders/s avg → ~1,200/s peak; 125K courier GPS writes/s (500K×¼Hz); ~3M live tracking conns (Little's Law 1,200/s×2,400s); e-commerce reads ~1000:1 over writes, ~10⁶ views/s peak needs >99% cache hit ratio. No quiz issued — authoring session. -->

2026-08-03 | RADIO derived-numbers drill (study pass) — self-quiz on today's own new material: capacity derivations for food-delivery & e-commerce, the two central splits, hot/cold courier entity split, the >99% cache-hit-ratio arithmetic, and the money-path idempotency + guarded-decrement mini-design | own notes (`docs/RADIO_FRAMEWORK.md`, `interviews/food-delivery/radio-walkthrough.md`, `interviews/e-commerce/radio-walkthrough.md`)
<!-- 2026-08-03 SECOND run same day = automated check-in, no live user (a LIVE authoring session ran earlier today, entry above). Rather than a new topic, issued the P0 study pass on today's just-authored RADIO material: 5 RADIO-framed Qs (R capacity, A central split, D data model, O cache-ratio math, mini-design money path), each requiring a defended number → decision. Grounded in food-delivery/radio-walkthrough (231→1,200 orders/s, 125K GPS writes/s, ~3M conns via Little's Law) + e-commerce/radio-walkthrough (~1000:1 read ratio, ≥99% hit ratio). Model answers left in chat. Confirm/grade on next live run. -->

## Quiz Results
<!-- date | topic | score | weak points -->
2026-07-23 | Video Streaming | self-quiz issued, not graded (automated run) | 6 Qs with model answers left in chat for self-check
2026-07-24 | Ride-Sharing | self-quiz issued, not graded (automated run) | 5 Qs (4 conceptual + 1 mini-design) with model answers left in chat for self-check
2026-07-25 | CDN & Edge (review) | self-quiz issued, not graded (automated run) | 5 Qs (capacity math, routing, cache headers, surrogate keys, thundering herd) with model answers left in chat for self-check

2026-07-26 | Blob / Object Storage | self-quiz issued, not graded (automated run) | 5 Qs (erasure coding, multipart upload, metadata/data separation, read-after-write consistency, mini-design) with model answers left in chat for self-check

2026-07-27 | Social Feed | self-quiz issued, not graded (automated run) | 5 Qs (hybrid fan-out threshold, celebrity problem, Redis sorted set + why store IDs, real-time push + thundering herd, mini-design) with model answers left in chat for self-check

2026-07-29 | Distributed Key-Value Store | self-quiz issued, not graded (automated run) | 5 Qs (1: N/W/R strong-consistency condition + tradeoff pairs; 2: LWW clock-skew failure vs vector-clock concurrent detection; 3: sloppy quorum + hinted handoff risk; 4: Merkle-tree anti-entropy efficiency; 5: mini-design shopping cart on a Dynamo-style store) with model answers left in chat for self-check. Grounded in kv-store/questions.md Q13-Q22.

2026-07-28 | Seat Reservation / Ticketmaster | self-quiz issued, not graded (automated run) | 5 Qs weighted to today's NEW material (Level 9 QR/tickets + Level 10 frontend): (1) offline QR validation design, (2) real-time seat-map lock push without broadcasting all 80k states, (3) client countdown vs authoritative server TTL, (4) two-tier hold-vs-booking concurrency, (5) mini-design QR issuance + no-double-ticket on issuance failure. Model answers left in chat for self-check. NOTE: earlier live authoring session today built simple-diagram + diagrams + Q43–Q53; this is the study/quiz pass on that material.

2026-08-02 | Food Delivery | self-quiz issued, not graded (automated run) | 5 Qs (4 conceptual + 1 mini-design), weighted to the 5 architecture images he authored today: (1) the three traffic paths and why each has different consistency/scaling needs [Q2]; (2) real-time "sold out" propagation to browsers + preventing the order [Q8], incl. browse-vs-checkout consistency split [Q9]; (3) prep-aware just-in-time dispatch — why assign when food is nearly ready, not at placement [Q25]; (4) authorize-then-capture — why not charge immediately, and WHEN to capture [Q18]; (5) mini-design: event-driven saga + transactional outbox keeping payment/inventory/restaurant/dispatch consistent when any step fails, incl. exactly-one-order idempotency [Q22/Q17]. Model answers left in chat for self-check. Grounded in food-delivery/questions.md.

2026-08-03 | RADIO derived-numbers drill | self-quiz issued, not graded (automated run) | 5 RADIO-framed Qs (Q1 R: derive avg/peak order rate + 125K GPS writes/s + ~3M conns via Little's Law → decision; Q2 A: food-delivery three-paths vs e-commerce consistency-gradient central splits + why "paths" vs "gradient"; Q3 D: hot/cold courier entity split, location store type + partition key + what breaks in the txn DB; Q4 O: the ≥99% cache-hit-ratio arithmetic (10⁴/10⁶) + stampede failure mode + coalescing/jitter/SWR; Q5 mini-design: POST /orders idempotency key placement + guarded conditional decrement + 409 loser + outbox). Model answers left in chat. Grounded in today's radio-walkthrough.md files.

## Gaps / Revisit
<!-- topics needing review -->
- CDN & Edge — CORRECTION: deep-dive.md DOES exist and is complete (51KB, README + 37 questions + answers). Reframed from "missing" to "review pending." Self-quiz issued 2026-07-25; grade live next session. Revisit if quiz reveals weak spots on: origin shield topology (Q25/B), multi-CDN traffic steering (Q30-31), cache poisoning (Q32).
- Blob Storage — P2, Not Started; direct dependency of video streaming.
- Deep-dives pending: cdn-edge, url-shortener, seat-reservation, web-crawler, search-autocomplete.
- Ride-Sharing: `deep-dive.md` exists but not confirmed reviewed live — revisit surge pricing + geospatial cell-size tradeoffs on next live session.
- Blob Storage: quiz issued 2026-07-26 (grade live next session). Revisit if shaky on erasure-coding overhead math (1.5x vs 3x), multipart resumability, and S3 strong read-after-write nuances (list-after-write, deletes).
- Social Feed: quiz issued 2026-07-27 (grade live next session). Revisit if shaky on: hybrid fan-out threshold reasoning (~10K followers, and why it's dynamic not static), viral-user push→pull transition (Q24), celebrity-tweet deletion propagation with pull model (Q23), and WebSocket session affinity at 10M connections (Q33). `deep-dive.md` exists — confirm reviewed live.
- KV Store: quiz issued 2026-07-29 (grade live next session). Revisit if shaky on: why W+R>N alone is *not* linearizability (only overlap, not real-time ordering — mention read-repair + sync replication), sloppy-quorum durability window vs hinted-handoff loss, vector-clock sibling explosion + when to prune, and Merkle-tree granularity/leaf-count tradeoff. `deep-dive.md` complete — confirm reviewed live.
- ~~BACKLOG: Seat Reservation Q43–Q53 answers unwritten~~ **RESOLVED / STALE (verified 2026-08-02 automated run):** answers.md (117KB, updated 2026-07-29) DOES contain A43–A53 in full — Level 9 (A43 QR issuance, A44 offline anti-fraud validation, A45 storage/delivery/re-issue, A46 notification flow, A47 issuance-failure) and Level 10 (A48 render 80k SVG/Canvas/WebGL, A49 real-time lock fan-out, A50 checkout/countdown, A51 offline QR/wallet, A52 accessibility, A53 performance). The folder is complete. Backlog item removed.
- BACKLOG (authoring, not a quiz gap): **4 gap pattern files** identified 2026-08-06 but not yet written to the depth of the existing 8 — **proximity/geospatial search**, **counting & top-K at scale**, **scheduled & delayed execution**, **search & ranked retrieval**. Outlines, ladders, reverse index, and AWS realization are already in `patterns/README.md` §Gaps + `docs/AWS_SERVICE_MAP.md` §2; only the full write-ups are missing. Note the strong correlation: these gaps cluster on the *unbuilt* ROADMAP topics (ad-click aggregation, leaderboard, job-scheduler, maps, search-engine) — so **build the pattern file first, then the topic**.
- BACKLOG (authoring): **one-page master diagram** is now a required final section of every `diagrams.md` (`docs/instructions.md` §2.1). Built for `payment-system` + `video-streaming` (exemplars, mermaid-render-verified 2026-08-06); the other ~19 done topics still need theirs. Also pending: back-fill the 4-column primitive/AWS/native service cheat-sheet + Patterns-in-play README table into existing topics (only `video-streaming` has an AWS table today).
- Seat Reservation: quiz issued 2026-07-28 (grade live next session). Folder COMPLETE — answers A43–A53 present in answers.md as of 2026-07-29 (earlier "answers missing" note was stale; corrected 2026-08-02 automated run). Revisit if shaky on: offline QR validation (signed rotating token + gate holds signing key & revocation list + scan-once), pushing per-section WebSocket deltas instead of all 80k seat states, client countdown reconciled to server TTL, and the hold-expiry-vs-payment-in-flight race (Q23 fencing).
- Food Delivery: quiz issued 2026-08-02 (grade live next session). `deep-dive.md` complete — confirm reviewed live. Revisit if shaky on: (a) the prep-time estimate as the linchpin of dispatch — why an inaccurate prep ETA breaks the whole JIT-assignment tradeoff (Q29), (b) order batching / stacked deliveries — when the second order's detour is worth it vs the freshness/late risk (Q27), (c) composite ETA continuously recomputed across prep+pickup+delivery legs (Q31), (d) transactional-outbox mechanics — why dual-write to DB+Kafka is unsafe and how the outbox+CDC fixes it (Q22), and (e) courier no-response reassignment without delaying food (Q28). NOTE: Q43-level frontend (Level 9) not covered in this quiz — candidate for a frontend-focused follow-up given his front-end lean.
- NEXT-UP per 7/29 evidence ranking (`next-topics-frequency-ranking.md`): Ad Click Aggregation (high freq, 4/4 agents, Alex Xu Vol 2 Ch.6) — not yet built. Candidate for an authoring run.
<!-- 2026-08-02 SECOND automated run (evening): the daily food-delivery check-in already ran earlier today (14:29 entry above), so instead of duplicating a quiz this run did housekeeping. Verified the Seat Reservation "answers unwritten" backlog was STALE — A43–A53 are fully written in answers.md (117KB, mtime 2026-07-29); corrected both stale lines above. No new topic studied, no live user. Outstanding for next LIVE session: (1) GRADE the food-delivery self-quiz (8/02) and the still-ungraded quizzes for CDN/Blob/Social Feed/KV Store/Seat Reservation; (2) then either revisit a graded gap or START the next-up topic Ad Click Aggregation (evidence-ranked #1 remaining). -->

## Suggested Next Session
- **Priority 0 — GRADE the 2026-08-03 RADIO derived-numbers self-quiz** (issued this automated run; model answers in chat). This is the fastest way to confirm the P0 "derive the numbers cold" goal actually landed. If any of the four derivations (231→1,200 orders/s · 125K GPS writes/s · ~3M conns via Little's Law · the 10⁴/10⁶ ⇒ ≥99% hit-ratio math) needed the model answer to recall, drill that one out loud from the RADIO checklist in `docs/RADIO_FRAMEWORK.md` §7 with no notes until it flows.
- **Priority 0b — drill the two RADIO walkthroughs out loud (added 2026-08-03).** Every number must end in "…therefore [decision]." Then whiteboard each topic from the §7 checklist cold.
- **Priority 1 — grade the backlog of self-quizzes.** Several quizzes (Food Delivery 8/02, KV Store 7/29, Seat Reservation 7/28, Social Feed 7/27, Blob 7/26, CDN 7/25) were issued in automated runs but never graded live. A quick live pass grading these will surface the *real* gaps instead of assumed ones.
- **Priority 2 — start Ad Click Aggregation** (evidence-ranked #1 remaining topic; Alex Xu Vol 2 Ch.6). Ties into his data-pipeline/streaming knowledge; a good next build once the quiz backlog is graded.

2026-08-04 | Caching & Replication cross-cutting layer (LIVE authoring) — every interview topic now carries BOTH a "🔁 Redundancy & Replication" and a "🗄️ Caching Strategy" per-system callout, backed by a new caching use-case matrix that mirrors the replication one row-for-row.
<!-- 2026-08-04 LIVE session with user. Request: "check if each section in interviews/ has caching-strategy and replication-strategy sections; if not and possible, add them." SURVEY FINDING that drove scope: the 🔁 appendix existed in only 14/29 folders, and fundamentals/Use_Cases_for_Redundancy_and_Replication.md closed by CLAIMING "each mapped folder's deep-dive.md ends with" that callout — false for 3 folders it explicitly maps (consensus, recommendation-system, food-delivery). Caching had NO fundamentals doc and ZERO per-topic callouts anywhere; concept depth existed (key-technologies-notes §22, interviews/distributed-caching/) but nothing tied a given system's caching decision back to it. User chose FULL scope (all 3 options) + "write a short why-not note" for non-applicable layers rather than skipping them. DELIVERED: (1) NEW fundamentals/Use_Cases_for_Caching.md — deliberately mirrors the replication doc's structure (hit/miss diagram, §A metrics, §B six write strategies, §C five invalidation strategies, §D jargon decoder, §E 5-question decision guide + PRD template, §2 17-system matrix, §2b "caches that aren't in the 17", §3 repo map, recall card). KEY DESIGN CHOICE: rows 1–17 are the SAME systems in the SAME order as the replication matrix, so row N is the same product in both docs and the pair reads together. (2) 🗄️ Caching Strategy callout appended to ALL 29 topic folders (Layers / Strategy / Invalidation / Why here) — sse has no deep-dive.md so it went in sse-deep-dive-qa.md. (3) 🔁 callout added to the 15 folders missing it (the 3 mapped + 12 unmapped). (4) Replication doc extended with a §2b "replication decisions that aren't in the 17" table (14 pattern folders), repo-map rows for them, a companion-doc pointer, and its closing claim corrected to "every topic folder." (5) key-technologies-notes §22 cross-linked to the new doc + the two dials. (6) Normalized section ORDER to 🔁-then-🗄️ across all 29 (the 15 new ones initially landed reversed; swapped via script). VERIFIED: 29/29 both callouts, zero broken relative links. FRAMING SPINE used throughout, worth reusing verbally: "a replica is a copy you may TRUST; a cache is a copy you may LOSE — losing a replica is a durability incident, losing a cache is a capacity incident." Plus: origin load = QPS×(1−hit_ratio) so 99% of 10⁶ still leaves 10K QPS on the DB ("the last nine is the one that saves you"); "you replicate a cache to protect your DATABASE, not the data"; "immutable content → version the key and never invalidate" (file-storage chunks, video segments, autocomplete trie hot-swap, e-commerce product body are all one pattern); "if you can cache it, it isn't money." NON-APPLICABLE cases written as why-not notes, and these are the highest-signal ones: consensus (a cached committed value is an unverified replica — only safe behind a ReadIndex/lease "freshness proof"), distributed-transactions (cache is an unaudited transaction participant = the dual-write problem; delete-after-commit via CDC outbox, never update), payment-system (an EVICTED IDEMPOTENCY KEY IS A DOUBLE CHARGE — needs noeviction, separate from cache state), api-design (replication lag is a CONTRACT problem: 201-then-404 from a read replica is fixed by returning the entity, not by infra), observability (Prometheus HA = duplicated independent scraping deduped at query time, NOT replication — deliberately breaks the repo's own pattern because a metric gap isn't a correctness bug). No quiz issued — authoring session. NEXT-SESSION HOOK: this material has never been quizzed; a good P0 study pass would be "for any 3 topics, state the caching layers/strategy/invalidation and the replication pattern/mode cold, then justify why they differ." -->
